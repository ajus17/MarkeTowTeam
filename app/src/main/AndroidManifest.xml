<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.adolfoursa.marketow">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
        android:name=".Producto"
        android:label="@string/addProducto"
        android:screenOrientation="sensorPortrait">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity
        android:name=".acceso"
        android:label="@string/addComercio" />
    <activity
        android:name=".addComercio"
        android:label="@string/addComercio" />
    <activity
        android:name=".carrito"
        android:label="Carrito de Compra"/>
    <activity
        android:name=".dellivery"
        android:label="Dellivery">
    </activity>
</application>

</manifest>